int main (int argc,char *argv)   /* Main function accepts arguments */
{
	int *c,y;
	z = 2.5 + x;
	print(x, y, z);
	if(y > 6 )
	{
		x = x + 67;
		z = 2.5 + x;
	}
	else 
	{
		if(!x)   /*	  This is nested if   */
		{
			y = y + 20;
			printf("If block");
		}
		else 
		{
			y = y + 20;
			printf("Else block");
		}
	}
}